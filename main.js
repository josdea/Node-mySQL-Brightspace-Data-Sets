

var createtables = require('./createtables');  //creates empty tables if they don't exist

var unzip = require('./unzip');  //unzips all files in the data zip folder into the data csv folder

var truncatetables = require('./truncatetables') // keeps table structure but deletes all data from the table


